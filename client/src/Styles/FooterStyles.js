import styled from 'styled-components';

export const StyledFooter = styled.footer`
  background-color: #1a4086;
  padding: 2em 0 2em;
  border: none;
  margin: 0;
  color: white;

  & > p > a {
    color: white;
    font-weight: 700;
  }

  & > p {
    margin-bottom: 0;
  }
`;