import styled from 'styled-components';

export const StyledFooter = styled.footer`
  background-color: #1a4086;
  padding: 3em 0 3em;
  border: none;
  margin: 0;
  color: white;

  & > p > a {
    color: white;
    font-weight: 700;
  }
`;